# Node / extension build
node_modules/
dist/
artifacts/
*.vsix
.vscode-test/

# .NET sample solution
samples/**/bin/
samples/**/obj/
samples/**/.vs/
samples/**/*.db
samples/**/*.db-shm
samples/**/*.db-wal

# macOS
.DS_Store

# Claude Code local settings
.claude/settings.local.json
CLAUDE.md

# Qwen Code local data (session memory, tmp, worktrees) — never commit, never publish
.qwen/

# Private (local-only) notes — never commit, never publish
/NOTES.private.md

